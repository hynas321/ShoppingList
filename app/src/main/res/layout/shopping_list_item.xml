<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/frameLayout"
    android:layout_width="match_parent"
    android:layout_height="56sp"
    android:layout_marginLeft="10sp"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/shopping_list_item_icon"
        android:layout_width="59dp"
        android:layout_height="45dp"
        android:contentDescription="@string/app_name"
        android:src="@mipmap/ic_launcher"
        tools:srcCompat="@tools:sample/avatars" />

    <TextView
        android:id="@+id/shopping_list_item_textView_name"
        android:layout_width="234dp"
        android:layout_height="wrap_content"
        android:text="aaa"
        android:textSize="@dimen/textView_title_textSize" />

    <ImageView
        android:id="@+id/shopping_list_item_ellipsis_icon"
        android:layout_width="89dp"
        android:layout_height="45dp"
        android:layout_weight="1"
        app:srcCompat="@drawable/ellipsis" />

</LinearLayout>